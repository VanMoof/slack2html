{
  "name": "utils",
  "version": "1.0.0",
  "description": "In this folder there's one JS script that parses the slack2html output in one or multiple folders, and applies a `nested` css class to thread messages so they are nicer to read on the screen",
  "main": "makenestedmessages.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12"
  }
}
